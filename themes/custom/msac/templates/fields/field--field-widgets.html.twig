{#
/**
 * @file
 * Theme override for a field. Removes extra markup.
 *
 *
 * @see template_preprocess_field()
 */
#}

{% for item in items %}
  {# <div class="paragraph-widget paragraph-widget--{{ item.content['#paragraph'].getType() }}"> #}
    {{ item.content }}
  {# </div> #}
{% endfor %}